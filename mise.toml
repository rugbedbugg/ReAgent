[tools]
python = "3.11"
uv = "latest"

[env]
_.python.venv = { path = ".venv", create = true }

[tasks.install]
description = "Install the package with dev and literature extras into the project venv"
run = "uv pip install -e '.[dev,literature]'"

[tasks.test]
description = "Run the test suite"
run = "pytest"

[tasks.lint]
description = "Run Ruff over the project"
run = "ruff check ."
